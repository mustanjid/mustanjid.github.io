---
import {getEntry} from "astro:content"
import ResearchLayout from "@/layouts/Research.astro";

const author = await getEntry('authors', 'default')
const {Content} = await author.render();
---

<ResearchLayout>
</ResearchLayout>

---
import {getEntry} from "astro:content"
import AuthorHero from "@/layouts/AuthorHero.astro";

const author = await getEntry('authors', 'default')
const {Content} = await author.render();

---

<AuthorHero content={author.data}>
	<Content />
</AuthorHero>